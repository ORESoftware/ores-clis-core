[package]
name = "ores-clis-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MIT"
description = "Shared terminal, color, log-level, structured-output, and streaming policy for ORESoftware Rust CLIs"
repository = "https://github.com/ORESoftware/ores-clis-core"
readme = "README.md"
keywords = ["cli", "terminal", "logging", "json", "opentelemetry"]
categories = ["command-line-utilities", "development-tools"]

[lib]
name = "ores_clis_core"
path = "src/root.rs"

[dependencies]
serde_json = "1"

[target.'cfg(unix)'.dependencies]
signal-hook = "0.4"

[target.'cfg(windows)'.dependencies]
ctrlc = "3.5"

[dev-dependencies]

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[lints.clippy]
all = "deny"
pedantic = "allow"
nursery = "allow"
